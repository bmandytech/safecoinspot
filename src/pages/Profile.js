import React from 'react';

const Profile = () => {
  return (
    <div className="min-h-screen bg-darkBg text-white flex items-center justify-center">
      <h1 className="text-3xl">Profile Page (Only Accessible When Logged In)</h1>
    </div>
  );
};

export default Profile;